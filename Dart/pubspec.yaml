name: dashboard_financas
description: Interface do Dashboard de Indicadores Financeiros Publicos.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  http: ^1.6.0
  provider: ^6.1.2